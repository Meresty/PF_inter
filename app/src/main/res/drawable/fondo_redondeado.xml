<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/gris" /> <!-- Fondo -->
    <corners android:radius="30dp" />
    <stroke
        android:width="4dp"
        android:color="@color/gris" /> <!-- Borde -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp" /> <!-- Espaciado -->
</shape>